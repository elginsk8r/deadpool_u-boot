if TARGET_C1_NQ_BX

config SYS_SOC
	default "c1"

config SYS_BOARD
	default "c1_nq_bx"

config SYS_VENDOR
	default "amlogic"

config SYS_CONFIG_NAME
	default "c1_nq_bx"

config DTB_BIND_KERNEL
    bool "load dtb from boot part but not flash rsv"
    default n

endif
